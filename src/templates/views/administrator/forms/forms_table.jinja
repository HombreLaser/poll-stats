{% for form in page %}
    <tr>
        <td>
            {{ form.created_at }}
        </td>
        <td>
            {{ form.name }}
        </td>
        <td>
            {{ form.status }}
        </td>
        <td>
            <a href="#" class="btn btn-sm btn-primary">
                Editar
            </a>
        </td>
    </tr>
{% endfor %}