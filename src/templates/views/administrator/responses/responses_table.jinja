{% for response in page %}
    <tr>
        <td>
            {{ response.created_at }}
        </td>
        <td>
            {{ response.form.name }}
        </td>
        <td>
            <a class="btn btn-sm btn-primary" href="{{ url_for('administrator_responses_controller.show', response_id=response.id) }}">
                Ver
            </a>
        </td>
    </tr>
{% endfor %}
