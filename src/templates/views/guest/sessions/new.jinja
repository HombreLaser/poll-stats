{% extends 'views/guest/base.jinja' %}
{% import 'macros/form.jinja' as form_renderer %}

{% block title %}Inicio de sesión{% endblock %}

{% block form_title %}
    Login                     
{% endblock %}

{% block form %}
    <form action="{{ url_for('sessions_controller.create') }}" method="post">
        {{ form.csrf_token }}

        {% if form.errors[None] %}
            <div class="p-3 mb-2 bg-danger text-light rounded">
                {{ form.errors[None][0] }}
            </div>
        {% endif %}

        <div class="form-floating mb-3">
            {{ form_renderer.render_field(form.email) }}
        </div>
        <div class="form-floating mb-3">
            {{ form_renderer.render_field(form.password) }}
        </div>
        <div class="form-check mb-3">
            {{ form_renderer.render_field(form.remember) }}
        </div>
        <div class="d-flex justify-content-end mt-4 mb-0">
            <input id="login_button" type="submit" class="btn btn-primary" value="Iniciar sesión" />
        </div>
    </form>
{% endblock %}
