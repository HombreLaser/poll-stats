{% macro render_field(field) %}    
    {% if field.errors %}
        {{ field(class_='is-invalid', required=False) }}
        <ul class="errors">
            {% for error in field.errors %}
                <li>{{ error }}</li>
            {% endfor %}
        </ul>
    {% else %}
        {{ field }}
    {% endif %}
    {{ field.label }}
{% endmacro %}